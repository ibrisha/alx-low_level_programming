#include "main.h"
/**
 * mul - multiplies two integers.
 * takes two integers as input.
 *@a:input integer.
 *@b:input integer.
 * Return:product of multiplication.
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
